# UNLAUNCHED

UNLAUNCHED is an interactive gallery where different artworks hide a secret phrase. Each piece reveals one word, and you are 
responsible for discovering them and uncovering the phrase.

Built with Rive and integrated into a Next.js experience, the 'impossible factor' lies in a user interface that behaves 
like a living system across every one of its layers. From the background layer — featuring ash in motion — to the foreground layer, 
every element is capable not only of coming to life but also of conveying a certain sense of depth. It is not just an UI 
with moving animations; it is an experience with a goal and a purpose.

The concept aims to convey a dystopian atmosphere where the user — the main character — is trapped within their own UI experience
and must find a way to uncover the secret phrase. 

Will you be able to find it? Will you receive any help? What awaits you once you do? Who knows. Live the experience and find out for yourself.

## Setup

```bash
npm install
npm run dev
# → http://localhost:3000
```

## Deploy

```bash
npx vercel deploy
```

## Secret phrase (for testing)

`NOTHING EVER EXISTS BY ACCIDENT`

## Exclusive URL

Update `EXCLUSIVE_URL` in `src/lib/artworks.ts` with your real link.
